# GitHub Pages deploy for the Sphinx docs — OPT-IN, single-version.
#
# The docs BUILD is already checked on every PR (the `docs` job in ci.yml). This
# file only publishes the built site. To enable:
#   1. Settings > Pages > Source = "GitHub Actions".
#   2. Uncomment everything below.
# For versioned docs (a page per release), connect the repo to ReadTheDocs instead
# of using this workflow — RTD builds each tag/branch natively.
#
# name: Deploy Docs
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
#
# permissions: {}
#
# concurrency:
#   group: pages
#   cancel-in-progress: false
#
# jobs:
#   deploy:
#     runs-on: ubuntu-24.04
#     timeout-minutes: 10
#     permissions:
#       pages: write
#       id-token: write
#       contents: read
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
#       - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
#         with:
#           python-version: "3.12"
#           cache: pip
#           cache-dependency-path: pyproject.toml
#       - run: pip install -e '.[docs]'
#       - run: sphinx-build -W -b html docs docs/_build/html
#       - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
#       - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
#         with:
#           path: docs/_build/html
#       - id: deployment
#         uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5.0.0
